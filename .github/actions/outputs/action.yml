---
name: "Outputs"
description: "Make some outputs"
outputs:
  foo:
    description: "Value of foo"
    value: ${{ steps.check.outputs.foo }}
  bar:
    description: "Value of bar"
    value: ${{ steps.check.outputs.bar }}
runs:
  using: "composite"
  steps:
    - name: "Output"
      id: check
      shell: bash
      run: |
        echo "foo=true" >> "$GITHUB_OUTPUT"
        echo "bar=false" >> "$GITHUB_OUTPUT"
